[package]
name = "mindustry-tests"
edition.workspace = true
license.workspace = true
version.workspace = true

[dependencies]
mindustry-core = { path = "../core" }
mindustry-desktop = { path = "../desktop" }
mindustry-server = { path = "../server" }
